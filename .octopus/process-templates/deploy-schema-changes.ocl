name = "deploy-schema-changes"
description = <<-EOT
        Runbook template used to deploy schema changes using:
        - Liquibase
        EOT

icon {
    color = "#EF6F1F"
    id = "exchange-alt"
}